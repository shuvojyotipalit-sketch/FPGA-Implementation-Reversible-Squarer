module half_adder_peres (
    input  A, B,
    output Sum, Cout
);
    wire M_garbage; // pass-through of A — not a useful output
    peres_gate PG (
        .U(A), .V(B), .W(1'b0),
        .M(M_garbage), .N(Sum), .O(Cout)
    );
endmodule
